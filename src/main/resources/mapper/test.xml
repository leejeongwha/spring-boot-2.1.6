<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.next.mooc.repository.TestRepository">
    <select id="selectAll" resultType="org.next.mooc.model.AAA">
        SELECT
        	id
        	,locale as cont
        FROM device
    </select>
</mapper>